=== ln
Creación de enlaces duros/simbolicos.
+
[unordered.stack]
-s:: `ln -s` Creación de enlace simbólico
-f:: `ln -f` Creación de enlace forzado al destino
-i:: `ln -i` confirmación antes de sobreescribir un enlace ya existente
